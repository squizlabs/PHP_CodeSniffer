<?php
$bar = (bool) $foo;
$bar = (bool) $foo;
$bar = (Bool) $foo;
$bar = (bool) $foo;
$bar = (int) $foo;
$bar = (int) $foo;
$bar = (INT) $foo;

// Test recognition with whitespace within the cast.
$bar = (  bool  ) $foo;
$bar = ( int ) $foo;
$bar = (     bool   	    ) $foo;
$bar = (  int) $foo;
